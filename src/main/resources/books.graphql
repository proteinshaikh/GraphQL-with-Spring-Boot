schema {
    query: Query
}

type Query {
    allBooks: [Book]
    book(id: String): Book
    allStudent:[Student]
}

type Book {
    isn: String
    title: String
    publisher: String
    authors: [String]
    publishedDate: String
}

type Student{
    id:String
    name:String
    address:String
}